// Crie uma variável player e atribua um objeto contendo as variáveis listadas abaixo:

let player = {
	name:'Marta',
	lastName:'Silva',
	age: 34,
	medals: { golden: 2, silver: 3 },
}

console.log( 'A jogadora ' + player.name + ' ' + player.age + ' anos ganhou ' + player.medals.golden + ' medalhas de ouro e ' + player.medals.silver + ' de prata' );